import React from 'react';
import Cursor from '../../Components/Cursor/Cursor';

const Dashboard = () => {
    return (
        <div>
            <Cursor/>
        </div>
    );
};

export default Dashboard;